name: test CI

on:
  push:
    branches: [ "main", "master" ]

jobs:
  build:
    runs-on: self-hosted    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: print hello
        run: |
          echo "Hello world"
